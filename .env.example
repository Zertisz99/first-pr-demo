# Local Postgres, provided by `npx prisma dev --name athleticore --detach`.
# Run `npx prisma dev ls` to see the current connection strings for this machine.
DATABASE_URL=
SHADOW_DATABASE_URL=

# Auth.js — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=
