# first-pr-demo

A tiny repo for practicing the GitHub pull request workflow.

## What is this?

This repo exists so you can open your first pull request with a real,
end-to-end flow: clone, branch, commit, push, and merge.

## Usage

There's nothing to install. Just look around, make a small change on a
branch, and open a PR.
